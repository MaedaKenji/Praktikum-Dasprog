#include <stdio.h>

int main() {
    int angka[10];  // Array untuk menyimpan 10 angka
    int positif = 0, negatif = 0, nol = 0;  // Variabel untuk menghitung jumlah positif, negatif, nol
    int total = 0;  // Untuk menghitung jumlah semua angka
    float rata_rata;  // Untuk menghitung rata-rata

    // Algoritma program

    // Menampilkan hasil perhitungan
    printf("Jumlah angka positif:");
    printf("Jumlah angka negatif:");
    printf("Jumlah angka nol:");
    printf("Rata-rata:");

    // Menampilkan angka yang lebih besar dari rata-rata
    printf("Angka yang lebih besar dari rata-rata: ");
    

    // Menampilkan angka yang lebih kecil dari rata-rata
    printf("Angka yang lebih kecil dari rata-rata: ");

    return 0;
}
